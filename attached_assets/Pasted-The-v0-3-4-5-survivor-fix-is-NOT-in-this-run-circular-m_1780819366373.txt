The v0.3 §4.5 survivor fix is NOT in this run — circular-merge Non-Loss violation persists, worse. Three reciprocal pairs — R013↔R019, R015↔R022, R023↔R028 — and all six members are retired. Three obligations lost. The smoking gun is the schema: merge_records still carry singular retired_id ({retired_id: R013, survivor_id: R019, …}), not the v0.3 retired_ids[]. And note survivor_id: R019 for the R013 claim — the higher id survived, i.e. it's still the old per-claim logic (retire whichever is the "duplicate" in each directional claim), so a reciprocal pair retires both. The union-find + min-id + fail-closed guard from v0.3 simply isn't implemented. VER-rm-10 fails; VER-rm-03 fails.
The candidate pre-filter is also still not wired. Candidate sizes are the full row again — refine = 12 (all Row 1 parents), duplicate = 20 (all siblings), mean 14.7. So implementation ticket 1 (wire candidates.py to the DD) didn't land either. The strong recall is the free-text judge carrying it, not entity anchoring — the DD is still being ignored on the matching path.
So Run8 reflects neither outstanding implementation ticket. It's a clean pre-v0.3 matching run with a good judge and a good DD upstream, but the merge step is still the old code. Nothing here is a new defect and nothing contradicts the specs — the specs just haven't been applied to the runner yet.
Action — unchanged from after Run7, now with evidence of urgency:

Implement v0.3 §4.5 (union-find classes → min-id survivor → retired_ids[] → fail-closed Non-Loss assert). This is the data-loss fix; it's losing 3 requirements per run. Quick confirmation it landed: merge_records carry retired_ids[], the lower id survives each pair, and no requirement appears in two records' retired sets.
Wire candidates.py to the DD (Matching §4.1) — the candidates_considered-drops-below-full-row signal still hasn't appeared.

No spec work outstanding — both items are the agent applying specs that already exist. Re-run once §4.5 is in, and VER-rm-10 should go green (no both-retired pairs); that's the one I'd gate the next review on, because it's the one actively destroying data.