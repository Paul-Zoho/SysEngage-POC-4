Before any spec change — and I don't think a spec change is what's needed — the thing to verify is whether the build that produced Run14 actually contains the D-rm-7 candidate-set filter. Two ways that could be confirmed:

Check the implementation: does the candidate-sibling construction filter by requirement_type (and subject class) before the duplicate judge? v0.5 §4.1 ("Sibling set (D-rm-5, D-rm-7): filtered to the child's own requirement_type AND subject class") and §4.2 are explicit. If that filter isn't in the code, that's the bug, full stop.
The verification tests VER-rm-11 (test_subject_class_not_merged) and VER-rm-13 (test_cross_type_not_merged) — if those exist and pass, the code should block R029→R028. If they're not implemented, or failing, that's the gap. Run14 is effectively a live counterexample to VER-rm-13, so either the test is absent or it's not exercising this path.

My honest read: don't touch the spec. v0.5 already says precisely what should happen, and Run14 violates it, so re-specifying would just restate the rule. The action is implementation conformance — get D-rm-7 and D-rm-5's candidate-set filter actually into the matcher and confirm VER-rm-11/VER-rm-13 pass — then re-run. This is the same lesson as Run12's propagation gap, and it's now urgent enough that the spec→build verification for Matching needs to be a checked step, not assumed.
For the tracker, I'd record this as a conformance observation on F96, not a new finding: "Run14 (labelled v0.5) still produced cross-type merges R024→R022 and R029→R028 — D-rm-7/D-rm-5 candidate-set filter not enforced in the build; spec correct, implementation gap; VER-rm-13/VER-rm-11 evidently not gating. Historical-accessibility Constraint lost for the third consecutive run (R025/R037 → R028/R029 → R028/R029). F96 remains Open pending a build that passes VER-rm-13." That keeps the institutional record honest about the fact that the fix keeps being specified but not landing.
So, two questions, and they decide the next move:

Can you confirm whether the Run14 build actually implements the §4.1 requirement_type/subject-class candidate filter (or whether VER-rm-13/VER-rm-11 exist as tests)? That's the single fact that tells us this is conformance, not spec.
Given the historical-accessibility Constraint has now been lost three runs running, do you want me to add a targeted regression check to the tracker/spec calling it out specifically — i.e. make VER-rm-13 explicitly include the Constraint-into-Functional case as a named fixture — so the implementation has an unambiguous test it currently fails? That's the one spec-adjacent thing I'd consider worthwhile: not changing the rule, but pinning the recurring loss as a named test case so it can't keep slipping through.