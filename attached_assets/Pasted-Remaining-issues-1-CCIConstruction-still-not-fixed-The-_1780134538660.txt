Remaining issues
1. CCIConstruction — still not fixed. The Phase 3b AnalysisPass mechanism name is still "CCIConstruction" rather than "CellContentItemConstruction". This was noted in F60; the implementation correction hasn't been applied yet.
2. execution_status: "PartialSuccess" — not a canonical value. Rows 3 and 4 (P1500, P1501) report "PartialSuccess". The canonical execution_status enum is Completed / CompletedWithWarnings / Failed / Aborted. The correct value here is "CompletedWithWarnings" — both rows triggered domain_count_advisory which landed in execution_warnings, and ADVC-3c-01 fires when the domain count exceeds the upper bound. "PartialSuccess" is not a recognised value and will break the Pass 3d gate check.
3. Domain IDs renumbered — a schema integrity concern. Run 1 had D001–D023. Run 2 has D001–D024, with different names and different row counts. This is a FullRerun not an IdempotentRerun — the implementation ran fresh derivations rather than detecting the prior state and reporting IdempotentRerun. The CCI set hashes are identical to Run 1, so every row should have been detected as IdempotentRerun. This needs investigation: either the implementation doesn't have IdempotentRerun detection working yet, or the domain IDs from Run 1 were not visible in the database when Run 2 was executed (suggesting the migration to the new schema dropped the old domain data).
4. schema_id: "v2_11" still inconsistent with sysengage_ledger_version: "2.12". Unchanged from Run 1 — minor but not yet addressed.
5. Row 3 now has 5 Domains (was 4) and Row 2 now has 7 (was 6). The domain groupings themselves changed between runs — confirming this is a genuine fresh derivation run, not a rerun detection. This is AI non-determinism in action: the same CCIs produced different groupings. This is expected and exactly why the IdempotentRerun mechanism exists — but it's only effective when the prior run's domain state is visible to the mechanism.

Summary scorecard
IssueRun 1Run 2Statuscell_content_item_refs absent✗✓Fixedmode_active: "LPM"✗✓Fixedexecution_warnings placement✗✓Fixedorphaned_ccis_after_repair stale field✗PartialOld field still present alongside newCCIConstruction mechanism name✗✗Not fixedexecution_status: "PartialSuccess"n/a✗New — not canonicalIdempotentRerun detectionn/a✗Not working — all rows ran as FirstRunschema_id inconsistency✗✗Not fixed
Three issues need immediate attention before the next run: "PartialSuccess" → "CompletedWithWarnings", the IdempotentRerun detection (likely a database state visibility issue after the schema migration), and removing the stale orphaned_ccis_after_repair field.