That resolves it cleanly — and it confirms F98 is genuinely in the build (the _run_conjoined_decompose path exists and the hard-violation routing is live; it just wasn't triggered at Row 3 because nothing generated a two-verb predicate). Good.
And the orphan is benign in outcome: I checked, CCI-ROW3-C-How-002 ends up covered by a surviving Row 3 requirement (a sibling sharing that CCI), so no Non-Loss gap manifested. The repair didn't reinstate the rejected proposal, but the concern is voiced.
But the agent's description exposes a spec↔build divergence I need your call on, because it's a latent Non-Loss hole:
My v0.24 spec says all three hard compound forms decompose in place — I wrote "For a compound violation (conjoined-predicate / compound-object / compound-condition) the repair is automatic in-place decomposition." The build is narrower: only conjoined_predicate decomposes; compound_object (and presumably compound_condition) route to orphan pool → _run_repair. The gate is all(v.rule == "conjoined_predicate").
Why that's a hole, not just a style difference: orphan→repair only probabilistically recovers. It worked here solely because the rejected compound-object's CCI was shared with another requirement. Had that CCI been unique to the rejected proposal and repair missed it, the concern would have been silently dropped — exactly the Non-Loss exposure in-place decomposition was meant to close by construction.
The decision — reconcile one of two ways:

(A) Widen the build to the spec (no spec change — spec already says this). Extend the decompose gate from conjoined_predicate only to include hard compound_object (and compound_condition). A hard compound-object is separable by the detector's own logic — the inseparable case is the soft PLB-3d-01 edge that never reaches reject — so it's always safe to split. And since _run_conjoined_decompose is an AI repair prompt, not a mechanical string split, it produces naturally-phrased atomics ("provide visibility of X and Y" → two clean statements), so there's no awkward-phrasing downside. SW agent task only.
(B) Narrow the spec to the build (spec edit). Scope in-place decomposition to conjoined_predicate only; compound-object/condition stay on orphan→repair — but then add a Non-Loss guard: a hard compound whose orphaned CCI isn't re-covered becomes a recorded hard failure (the CHK-3d-10 discipline), not a silent drop.

I recommend (A): it's deterministic, closes the hole by construction, fits automate-everything, the spec already says it, and the AI-driven decompose handles phrasing — so the only reason the build limited it to conjoined_predicate (caution about splitting) doesn't actually bite. (A) means no spec change and a small SW-agent gate widening; (B) means a Row 3 §4.1.1(b) / Row 4 §4.3 edit plus the guard.
Which way — (A) widen build, or (B) narrow spec + guard?