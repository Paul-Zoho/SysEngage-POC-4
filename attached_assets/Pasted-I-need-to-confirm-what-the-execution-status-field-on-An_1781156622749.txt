I need to confirm what the execution_status field on AnalysisPass actually means in the ledger, because our spec says Completed but the exported ledger shows Success / PartialSuccess / Failed, and we've been reading this field to judge pass outcomes. Before we rely on it further — including treating R5_Run2's Row 5 RequirementDerivation: Failed as proof that CHK-3d-10 enforcement fired — I need the status pipeline characterised end to end.
You earlier noted (.agents/memory/sc-status-quirk.md) that the mechanism returns Success from a Source-Capture convention while __init__.py writes execution_status = "Completed" (line ~430) and the return dict (line ~511) maps it through. Please run this down and answer the following:
1. The write path vs the read path. For a RequirementDerivation pass, trace the value from where it's set to where it lands in the ledger export. Specifically: what literal string does __init__.py write to the analysis_pass.execution_status column, and what literal string appears in the ledger JSON export for that same pass? If they differ, identify the exact transform and the line that applies it (orchestrator, return-dict mapping, serializer, or a status-bleed from another mechanism).
2. The value vocabulary. Enumerate every value execution_status can take as actually persisted, and the condition that produces each. Our exports show at least Success, PartialSuccess, Failed. The spec vocabulary is Completed / CompletedWithWarnings / Failed / Skipped. Give the mapping between the two vocabularies (e.g. is Success ≡ Completed, PartialSuccess ≡ CompletedWithWarnings?), and whether the mapping is consistent across all mechanisms or differs by mechanism.
3. The critical question — is Failed reliable? This is the one that matters most. When a RequirementDerivation pass hits a hard failure (specifically: CHK-3d-10 finds persistent unrefined seeds after Path-R repair — the extinction case), what execution_status is written, and is it written through a distinct code path from the normal Success/Completed path? In R5_Run2, Row 5 RequirementDerivation shows Failed with path_r=0 and unrefined_count=26, while every other pass in that run shows Success. I need to confirm that Failed was produced by the CHK-3d-10 hard-failure path and is not an artifact of some other failure (e.g. an AI/parse error or the connection bug, which were both live around then). Show the code path that produced that specific Failed, and confirm whether downstream_rerun_required / the gap records corroborate it as a genuine CHK-3d-10 extinction stop.
4. PartialSuccess on Matching. In R5_Run9, RequirementMatching shows PartialSuccess at rows 2/3/5 but Success at row 4. What drives PartialSuccess for Matching — is it benign (e.g. no_candidates on row-native requirements, which is expected) or does it indicate an unhandled condition? I want to know whether PartialSuccess on Matching is a normal outcome or a warning I should be reading.
5. The fix decision. Recommend whether the right resolution is (a) make the ledger export emit the spec vocabulary (Completed/CompletedWithWarnings/Failed/Skipped) so spec and ledger agree, or (b) revise the spec to adopt the build's vocabulary (Success/PartialSuccess/Failed), or (c) leave both and document the mapping. Whichever you recommend, identify the single source of truth for the value so it can't diverge per-mechanism again.
What I need back: the write-string vs export-string for a RequirementDerivation pass; the full value vocabulary with conditions; the confirmed code path for R5_Run2's Row 5 Failed (CHK-3d-10 or not); the meaning of Matching PartialSuccess; and your fix recommendation with the single-source-of-truth location. With that, I can either confirm the CHK-3d-10 enforcement claim and close the item, or pin exactly what's misreporting.