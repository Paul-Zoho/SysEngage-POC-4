Row 2 derivation itself is conformant and correctly leveled. All three runs: types in triad, Non-Loss complete (every Row 2 CCI covered), and — importantly — the row-lens descent is working. Row 2 dropped the enterprise voice entirely (0/15, 0/21, 0/18 start with "the enterprise") and correctly shifted to the Owner/conceptual register: "Parents shall define household tasks," "The business shall reset task availability on weekly cycle boundaries," "The business shall maintain a record of the monetary value associated with each task." These are genuine elaborations of the Row 1 requirements, not duplicates — exactly what Row 2 should produce. Signals are stable at 15 across all three runs, same stabilisation pattern Row 1 reached. So the derivation engine handles a second row cleanly.
The blocker: the Requirement Matching service is not in the pipeline. No Matching pass ran in any of the three; the passes are SC + (RLSRA/CCI/DD/RD)×2 — one cycle per row, no Matching after either. Every Row 2 requirement has empty/absent refines_refs. This is the same omission class as the original RLSRA bug — a step left out of the orchestration, yielding structurally-valid but semantically-empty output that doesn't error.
And the semantic consequence is worse than "a field is empty." Per F86, an empty refines_refs at row n>1 is the novel-requirement gap signal. With Matching omitted, all 15/21/18 Row 2 requirements read as novel requirements with no Row 1 parent — so the downstream gap→question→answer mechanism would fire a clarifying question for every single Row 2 requirement, when in reality most of them refine an existing Row 1 requirement (e.g., Row 2's "Parents shall define household tasks" clearly refines Row 1's "establish task-based compensation structure"). It's a false-novel-gap flood, and because empty refines is a permitted state, nothing flags it. This is precisely the kind of silent failure worth catching before it reaches Phase 9.
This means the consistency experiment can't answer your question yet — for two reasons:

The propagation channel didn't execute. Under F83, the only way Row 1 divergence can reach Row 2 is through Matching/refines_refs. Matching didn't run, so there's zero propagation signal. All the divergence you can see across the three runs (Row 2 reqs 15/21/18, domains 9/10/10) is pure derivation noise — and it's doubled, because…
These are full Row 1+Row 2 pipelines, not fixed-baseline runs. Each run re-derived its own Row 1 (17/19/21 reqs) from scratch rather than using a blessed baseline. So even if Matching had run, you'd have Row-1 derivation noise and Row-2 derivation noise and baseline effect all varying together — the exact confound I flagged last turn. You couldn't attribute any divergence to the baseline.

Path forward:

Wire Matching into the runner — after RequirementDerivation, for every row n>1 (no-op at row 1 per VER-rm-02). This is the fix; it's the analogue of inserting RLSRA back into the Row 1 order.
Before attributing anything to baselines, confirm Matching is deterministic on a fixed baseline — run it twice against the same Row 1 set. If the same Row 2 requirement links to a different parent on repeat, that's a Matching defect (it's still v0.1, never exercised against a real parent layer), and it would contaminate the whole experiment. Rule that out first.
Then run the surgical version of your test: hold one Row 2 derivation fixed and run Matching against the three Row 1 baselines (Run12/13/14) separately. Now every refines_refs difference is attributable purely to the baseline — which is the propagation question you actually want answered. The metric is: does the same Row 2 requirement find a parent under one baseline but come up empty under another (i.e., did Run14's wording/granularity of a Row 1 requirement cause a match to miss)?

So: nothing wrong with the derivation, and the row-lens descent is genuinely encouraging. But these three ledgers measure Row 2's derivation noise, not cross-row propagation, because the cross-row link was never built. Fix Matching, confirm it's stable on a fixed baseline, then re-run the attribution cleanly. Send the next batch and I'll split within- vs between-baseline variance for you — assuming refines_refs is actually populated that time.